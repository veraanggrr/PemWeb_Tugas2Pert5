<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>edit data</title>
    <style>
      /* General styling */
      body {
        font-family: "Comic Sans MS", cursive, sans-serif;
        background-color: #f2f2f2;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: flex-start;
        align-items: center;
        flex-direction: column;
        min-height: 100vh; /* Mengubah dari height ke min-height */
        overflow-y: auto; /* Mengizinkan scroll vertikal */
      }

      /* Heading styling */
      h2 {
        color: #ff6f61;
        font-size: 2.5rem; /* Ukuran font yang lebih kecil */
        text-align: center;
        margin-bottom: 20px;
        animation: bounceIn 1.5s ease-out; /* Menambahkan animasi */
      }

      /* Form styling */
      form {
        background: white;
        padding: 20px;
        border-radius: 10px; /* Sudut bulat untuk form */
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
        transition: transform 0.3s; /* Transisi saat hover */
        animation: fadeIn 0.5s; /* Animasi saat muncul */
      }

      form:hover {
        transform: scale(1.02); /* Efek zoom saat hover */
      }

      /* Label styling */
      label {
        font-size: 1.2rem; /* Ukuran font yang lebih besar untuk label */
        color: #333; /* Warna label yang lebih gelap */
        margin-bottom: 5px; /* Ruang di bawah label */
        display: block; /* Menampilkan label sebagai blok */
      }

      /* Input field styling */
      input[type="text"],
      input[type="email"],
      input[type="tel"] {
        width: 80%;
        padding: 10px;
        margin: 8px 0;
        box-sizing: border-box;
        border: 2px solid #ff6f61;
        border-radius: 5px;
        transition: 0.3s;
        margin-bottom: 12px; /* Tambahkan margin agar lebih seimbang */
        font-size: 1rem; /* Ukuran font yang konsisten */
        background-color: #fff3e0; /* Warna latar belakang input */
      }

      input[type="text"]:focus,
      input[type="email"]:focus,
      input[type="tel"]:focus {
        border-color: #ff9f89;
        box-shadow: 0 0 8px rgba(255, 111, 97, 0.5);
      }

      /* Button styling */
      button {
        background-color: #ff6f61;
        color: white;
        border: none;
        padding: 10px 20px;
        margin-top: 20px;
        font-size: 1.2rem; /* Ukuran font yang lebih besar */
        cursor: pointer;
        border-radius: 5px;
        transition: all 0.4s ease;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
        animation: wobble 3s infinite ease-in-out;
      }

      button:hover {
        background-color: #ff9f89;
        transform: translateY(-5px) scale(1.05);
      }

      /* Keyframes for animations */
      @keyframes bounceIn {
        0% {
          transform: scale(0.8);
          opacity: 0;
        }
        80% {
          transform: scale(1.1);
          opacity: 1;
        }
        100% {
          transform: scale(1);
        }
      }

      @keyframes wobble {
        0%,
        100% {
          transform: rotate(-5deg);
        }
        50% {
          transform: rotate(5deg);
        }
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      /* Footer styling */
      footer {
        margin-top: 50px;
        font-size: 0.9rem;
        text-align: center;
        color: #888;
        animation: slideUp 1.5s ease-in-out;
      }

      @keyframes slideUp {
        from {
          transform: translateY(50px);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }
    </style>
  </head>
  <body>
    <h2>Edit Data</h2>
    <form action="/update/<%= users.id %>" method="post">
      <label for="Name">Name:</label>
      <input
        type="text"
        id="Name"
        name="Name"
        required
        value="<%= users.Name %>"
      /><br />
      <label for="email">email:</label>
      <input
        type="text"
        id="email"
        name="email"
        value="<%= users.email %>"
      /><br />
      <label for="phone">phone:</label>
      <input
        type="text"
        id="phone"
        name="phone"
        value="<%= users.phone %>"
      /><br />
      <button type="submit">Simpan</button>
    </form>
  </body>
</html>
